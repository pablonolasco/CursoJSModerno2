// String Methods
const producto = "Monitor 20 Pulgadas";

// Tamaño de una cadena
console.log(producto.length);

// Existe en la cadena ala palabra Existe = 0, No existe = -1
console.log(producto.indexOf("Monitor"));

// Existe en la cadena la palabra Existe = true, No existe = false;
console.log(producto.includes("monitor"));
